<header>
  <div class="wrapper">
    <a href="/" title="Besnik">
      <img class="logo" src="./images/besnik_logo.png" alt="besnik image" />
    </a>

    <div class="nav-wrapper">
      <nav class="nav">
        <button class="close-btn"> &times;</button>
        <ul class="nav-list">
          <li>
            <a title="home" class="nav-link" href="#">Home</a>
          </li>
          <li>
            <a title="features" class="nav-link" href="#">Features</a>
          </li>
          <li>
            <a title="service" class="nav-link" href="#">Service</a>
          </li>
          <li>
            <a title="listed" class="nav-link" href="#">Listed</a>
          </li>
          <li>
            <a title="contact" class="nav-link" href="#">Contact</a>
          </li>
        </ul>
      </nav>

      <div class="button-container">
        <button title="sign up">Sign Up</button>
        <button class="register-btn" title="register">Register</button>
      </div>
    </div>

    <button title="open menu" class="menu-btn">
      <div class="hamburger">
        <span class="line line-1"></span>
        <span class="line line-2"></span>
        <span class="line line-3"></span>
      </div>
    </button>
  </div>
</header>

<script>
  const menuBtn = document.querySelector(".menu-btn");
  const menu = document.querySelector(".nav-wrapper");
  const closeBtn = document.querySelector(".close-btn");

  menuBtn?.addEventListener("click", () => {
    menu?.classList.add("open");
  });

  closeBtn?.addEventListener("click", () => {
    menu?.classList.remove("open");
  });
</script>

<style>
  header {
    padding-top: 38px;
    background-color: #e6f0ff;
  }
  .wrapper {
    display: flex;
    align-items: center;
    max-width: 1210px;
    margin: 0 auto;
    padding: 0 16px;
  }

  .logo {
    width: 107px;
  }

  .nav-wrapper {
    flex: 1;
    display: flex;
    align-items: center;
    justify-content: space-between;
  }
  .nav {
    flex: 1;
  }

  .nav-list {
    list-style: none;
    display: flex;
    align-items: center;
    gap: 30px;
    padding-left: 64px;
    margin: 0;
  }
  .nav-link {
    text-decoration: none;
    color: #4f5361;
    font-size: 14px;
    font-style: normal;
    font-weight: 500;
    line-height: normal;
  }
  button {
    cursor: pointer;
    display: inline-block;
    background-color: transparent;
    border: none;
    color: #0f1b4c;
    font-size: 14px;
    font-family: Poppins;
    font-style: normal;
    font-weight: 500;
    line-height: normal;
  }
  .register-btn {
    border-radius: 8px;
    background: #0f1b4c;
    color: #ffffff;
    height: 40px;
    width: 98px;
    margin-left: 18px;
  }

  .menu-btn {
    display: none;
  }

  .close-btn {
    display: none;
  }

  @media screen and (max-width: 830px) {
    .nav-list {
      padding-left: 30px;
    }
  }

  @media screen and (max-width: 768px) {
    .wrapper {
      justify-content: space-between;
    }
    .nav-wrapper {
      position: fixed;
      top: 0;
      right: -250px;
      width: 100%;
      max-width: 250px;
      height: 100%;
      background-color: #ffffff;
      transition: right 0.3s ease-in-out;
      flex-direction: column;
      padding: 28px 16px;
      z-index: 500;
      box-shadow: rgba(149, 157, 165, 0.2) 0px 8px 24px;
    }
    .nav-wrapper.open {
      right: 0;
    }
    .nav {
      display: flex;
      align-items: center;
      justify-content: flex-start;
      flex-direction: column;
    }
    .nav-list {
      flex-direction: column;
      padding: 0;
    }
    .menu-btn {
      display: block;
      background-color: transparent;
    }
    .hamburger {
      display: flex;
      flex-direction: column;
      justify-content: center;
      width: 20px;
    }

    .line {
      width: 100%;
      height: 2px;
      background-color: #4f5361;
      margin-bottom: 4px;
    }

    .line-1 {
      width: 90%;
    }

    .line-3 {
      margin-bottom: 0;
      width: 80%;
    }

    .close-btn {
      display: flex;
      align-items: center;
      justify-content: center;
      height: 28px;
      width: 28px;
      font-size: 28px;
      line-height: 1;
      margin-bottom: 36px;
      border-radius: 5px;
      padding-top: 2px;
    }

    .close-btn:hover {
      background-color: rgba(15, 27, 76, 0.1);
    }
  }
</style>
